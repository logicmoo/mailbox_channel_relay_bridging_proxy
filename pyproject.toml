[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "mailbox-channel-relay-bridging-proxy"
version = "0.1.0"
description = "A durable mailbox and chat-channel relay bridging proxy"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "python-dotenv>=1.0",
  "requests>=2.32",
  "websocket-client>=1.8",
]

[project.optional-dependencies]
dev = ["pytest>=8"]

[project.scripts]
mailbox-server = "mailbox_channels.server:main"
mailbox-client = "mailbox_channels.agent_mailbox:main"
mailbox-console = "mailbox_channels.console_client:main"

[tool.setuptools]
package-dir = {"" = "src"}
include-package-data = true

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
mailbox_channels = ["resources/*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
